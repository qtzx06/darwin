# Copy to .env and fill in values
# Testnet fullnode URL (optional, defaults to public testnet)
SUI_RPC_URL=

# Published package ID of escrow_contracts on testnet, e.g. 0xabc... (required)
PACKAGE_ID=

# Gas budget per tx (mist)
GAS_BUDGET=50000000

# Two test accounts' private keys. Use `npm run generate-keys` to generate if needed.
ALICE_PRIVATE_KEY=
BOB_PRIVATE_KEY=
